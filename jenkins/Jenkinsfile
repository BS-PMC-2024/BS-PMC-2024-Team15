pipeline {
    agent {
        docker {
            image 'flask-api'
            args '-p 5000:5000'
        }
    }
    stages {
        stage('Test') {
            steps {
                sh 'pytest'
            }
        }
        stage('Deploy') {
            steps {
                sh 'python app.py'
            }
        }
    }
}
