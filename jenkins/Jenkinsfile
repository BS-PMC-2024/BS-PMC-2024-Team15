pipeline {
    agent none
    stages {
        stage('Build') {
            agent {
                docker {
                    image 'python:3.10-alpine'
                }
            }
            steps {
                sh 'python -m py_compile Backend/app.py'
            }
        }
        stage('Test') {
            agent {
                docker {
                    image 'python:3.10-alpine'
                }
            }
            steps {
                sh 'pip install pytest'  // Install pytest globally
                sh 'pytest -vv --disable-warnings --junit-xml test-reports/results.xml' // Run pytest
            }
            post {
                always {
                    junit 'test-reports/results.xml'
                }
            }
        }
    }
}
